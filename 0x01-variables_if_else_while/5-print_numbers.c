#include <stdio.h>

/**
 * main - start
 *
 * Description: 1-10
 * Return: 0 if it work, other if it didn't
 */

int main(void)
{
	int n;

	for (n = '0'; n <= '9'; n++)
		putchar(n);
	putchar('\n');
	return (0);
}
